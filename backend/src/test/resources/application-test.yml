spring:
  datasource:
    url: jdbc:h2:mem:rrhh;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    secret: 0123456789abcdef0123456789abcdef0123456789abcdef
    expiration-ms: 86400000
  cors:
    allowed-origin: http://localhost:5173
  seed:
    admin:
      full-name: Admin Test
      email: admin@grupomendoza.com
      password: Admin12345!
    demo:
      enabled: false
      default-password: Demo12345!
